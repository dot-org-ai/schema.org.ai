---
$id: https://schema.org/readonlyValue
$context: https://schema.org
$type: Property
name: readonlyValue
description: "Whether or not a property is mutable. Default is false. Specifying this for a property that also has a value makes it act similar to a \"hidden\" input in an HTML form."
title: readonlyValue
domainIncludes:
  - "PropertyValueSpecification"
rangeIncludes:
  - "Boolean"
---

# readonlyValue

Whether or not a property is mutable.  Default is false. Specifying this for a property that also has a value makes it act similar to a "hidden" input in an HTML form.

## Used on types

- [PropertyValueSpecification](PropertyValueSpecification.html)

## Expected types

- [Boolean](Boolean.html)

## Resources

- [Schema.org readonlyValue](https://schema.org/readonlyValue)

