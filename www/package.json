{
  "name": "schema.org.ai-www",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "gate": "node gate.mjs",
    "og": "node build.js --og",
    "dev": "npm run build && npm run gate && wrangler dev",
    "deploy": "npm run build && npm run gate && wrangler deploy"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsonld": "^9.0.0",
    "wrangler": "^4",
    "yaml": "^2.9.0"
  }
}
