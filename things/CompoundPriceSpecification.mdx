---
$id: https://schema.org.ai/CompoundPriceSpecification
$context: https://schema.org
$type: Class
name: CompoundPriceSpecification
description: A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. "electricity" or "final cleaning").
subClassOf: ["PriceSpecification"]
---

# CompoundPriceSpecification

A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. "electricity" or "final cleaning").

## Type Hierarchy

**CompoundPriceSpecification**



## Properties

See [Schema.org CompoundPriceSpecification properties](https://schema.org/CompoundPriceSpecification#properties)

## Usage in Business-as-Code

```typescript
import { $ } from 'sdk.do'
import type { CompoundPriceSpecification } from 'schema.org.ai'

// Create a CompoundPriceSpecification
const item: CompoundPriceSpecification = {
  $type: 'CompoundPriceSpecification',
  name: 'Example CompoundPriceSpecification'
}

// Use semantic patterns
await $.CompoundPriceSpecification.create(item)
const result = await $.CompoundPriceSpecification.get('item-id')
```

## Resources

- [Schema.org CompoundPriceSpecification](https://schema.org/CompoundPriceSpecification)
- [CompoundPriceSpecification Properties](https://schema.org/CompoundPriceSpecification#properties)

## License

This documentation is based on Schema.org vocabulary, which is licensed under Creative Commons Attribution-ShareAlike License (version 3.0).
