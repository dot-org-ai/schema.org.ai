---
$id: https://schema.org.ai/WebContent
$context: https://schema.org
$type: Class
name: WebContent
description: WebContent is a type representing all [[WebPage]], [[WebSite]] and [[WebPageElement]] content. It is sometimes the case that detailed distinctions between Web pages, sites and their parts are not always important or obvious. The  [[WebContent]] type makes it easier to describe Web-addressable content without requiring such distinctions to always be stated. (The intent is that the existing types [[WebPage]], [[WebSite]] and [[WebPageElement]] will eventually be declared as subtypes of [[WebContent]].)
subClassOf: ["CreativeWork"]
---

# WebContent

WebContent is a type representing all [[WebPage]], [[WebSite]] and [[WebPageElement]] content. It is sometimes the case that detailed distinctions between Web pages, sites and their parts are not always important or obvious. The  [[WebContent]] type makes it easier to describe Web-addressable content without requiring such distinctions to always be stated. (The intent is that the existing types [[WebPage]], [[WebSite]] and [[WebPageElement]] will eventually be declared as subtypes of [[WebContent]].)

## Type Hierarchy

[Thing](Thing.mdx) > [CreativeWork](CreativeWork.mdx) > **WebContent**

## Direct Subclasses

- **[HealthTopicContent](HealthTopicContent.mdx)**: [[HealthTopicContent]] is [[WebContent]] that is about some aspect of a health topic, e

## Properties

See [Schema.org WebContent properties](https://schema.org/WebContent#properties)

## Usage in Business-as-Code

```typescript
import { $ } from 'sdk.do'
import type { WebContent } from 'schema.org.ai'

// Create a WebContent
const item: WebContent = {
  $type: 'WebContent',
  name: 'Example WebContent'
}

// Use semantic patterns
await $.WebContent.create(item)
const result = await $.WebContent.get('item-id')
```

## Resources

- [Schema.org WebContent](https://schema.org/WebContent)
- [WebContent Properties](https://schema.org/WebContent#properties)

## License

This documentation is based on Schema.org vocabulary, which is licensed under Creative Commons Attribution-ShareAlike License (version 3.0).
